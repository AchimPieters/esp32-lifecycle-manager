idf_component_register(
    SRCS
        "main.c"
        "ota.c"
        "wifi_config.c"
        "wifi_config_util.c"
        "form_urlencoded.c"
    INCLUDE_DIRS
        "."
        "include"
        "content"
    PRIV_REQUIRES
        esp_https_ota
    REQUIRES
        freertos
        esp_wifi
        nvs_flash
        driver
        esp_event
        esp_netif
        esp_timer
        esp_http_client
        app_update
        mbedtls
        json
        http_parser
    EMBED_TXTFILES
        "content/index.html"
)

target_add_binary_data(${COMPONENT_TARGET} "github_root_ca.pem" TEXT)
